name: sudo authentication failure
type: frequency
index: filebeat-*

num_events: 1
timeframe:
  minutes: 5

filter:
- query:
    query_string: 
      query: "'sudo' AND 'authentication failure'"

alert:
- debug

alert_subject_args:
- "@timestamp"
- host.hostname
- host.os
- message
